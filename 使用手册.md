# 使用手册

> **Lodestone 是干啥的、命令清单、爬虫引擎怎么选、出错了怎么办 —— 全在这里。**
>
> 安装看 [安装说明](安装说明.md)；项目总览看 [README](README.md)。

---

## 这是什么

Lodestone 每天从 GitHub、HuggingFace、MCP Registry、arXiv 抓主流 AI 工具，按 20 个用途分类整理成中文友好仪表盘。

**5 个数据源**（互补独立，缺一个不影响其他）：

| 源 | 抓什么 | API |
|---|---|---|
| **GitHub** | 主流开源 AI 项目（分类 + 5k 池 + 趋势 + 手工种子） | `gh api` + `github.com/trending` HTML |
| **HuggingFace Spaces** | 社区精选 AI 应用 demo | `api/spaces?sort=likes7d` |
| **HuggingFace Models** | 模型权重排行 | `api/models?sort=likes7d` |
| **MCP Registry** | 官方 MCP server 注册表 | `registry.modelcontextprotocol.io/v0/servers` |
| **arXiv** | 最新 AI 论文（cs.AI / cs.CL / cs.LG） | `export.arxiv.org/api/query` |

GitHub 抗爬时 MCP / HF / arXiv 仍能完整工作 —— 你不会「今天啥都看不到」。

**20 个分类**：AI Agent & Skills · RAG / Memory · LLM Interface · Code Generation · Workflow · Multimodal · Fine-tuning · Eval · AI IDE · LLM Gateway · Observability · Awesome Lists · MCP Servers · Voice · Browser Use · HuggingFace Spaces · HuggingFace Models · AI 安全 · 机器人 / Embodied AI · arXiv 论文

---

## 三步上手

```bash
cd ~/lodestone

# 1. 抓数据（约 3-5 分钟；首次跑会拉 + 翻译 1k+ repo）
./radar.py crawl

# 2. 起后端 + 前端，一条命令搞定
./radar.py web                # 后台拉 serve + 自动开浏览器 → http://127.0.0.1:8765

# 3. 触发 /yz:ai 命令（自动起服务 + 调用宿主 LLM 生成 5 桶中文详细介绍）
```

没装 Claude Code？直接终端看：

```bash
./radar.py today              # Top 15 + 20 个分类概览
```

---

## 命令速查

| 命令 | 干什么 | 何时用 |
|---|---|---|
| `./radar.py web [port]` | 一键：起 serve + 自动开浏览器 | 日常看仪表盘（推荐） |
| `./radar.py crawl` | 抓所有数据源 + 翻译 + 入库 | 每天 / 新数据 |
| `./radar.py today` | 终端打印 Top 15 + 分类 | 快速看一眼 |
| `./radar.py serve [port]` | 只起 JSON API + 托管前端（默认 8765） | API-only / 调试 |
| `./radar.py audit` | 数据质量审计（去重 / 金融过滤 / 来源分布） | 月度自查 |
| `python3 refresh_snapshot.py` | 只刷 MCP / HF / 翻译（不动 GitHub） | GitHub 限流时 |
| `cd frontend && npm run dev` | 起 Vite + Python（dev 热更新） | 改前端代码 |

## API 端点（serve 在跑时）

| 方法 | 路径 | 返回 |
|---|---|---|
| GET | `/api/data` | `{hot_now, categories, fetched_at}` 主视图 |
| GET | `/api/local` | 本机已装的 skills / commands / agents / plugins |
| GET | `/api/top?page=&size=&sort=` | 1k+ AI 主流项目池（按 stars/recent/name 排序） |
| GET | `/api/gain?size=&min_delta=` | 24h 星增榜 |
| GET | `/api/stats` | 语言 × 主题分布 |
| GET | `/api/scrapers/status` | 当前可用爬虫引擎 |
| POST | `/api/crawl` | 后台触发 crawl（忙时返 409） |
| POST | `/api/install` | 克隆 GitHub repo 装为 skill |
| POST | `/api/install-cli` | 把任意 CLI 包成 slash command |

所有 API 只绑 `127.0.0.1` —— 因为能 `git clone` 到你机器的 skill 目录，暴露到内网等于送权限。需要 LAN 访问：`RADAR_HOST=0.0.0.0 .env`，外面套 nginx。

---

## 数据怎么走（爬取流程）

```
./radar.py crawl
   │
   ├─[1] 文件锁（data/crawl.lock）—— 防并发
   │
   ├─[2] GitHub GraphQL 批量搜索（204 个 query × N aliases/请求）
   │      失败 → REST gh_search → HTML 阶梯兜底
   │
   ├─[3] 5 个非 GitHub 数据源并行抓：
   │      HF Spaces · HF Models · MCP Registry · arXiv · GitHub Trending
   │
   ├─[4] AI 过滤（topic + 描述关键词 + 金融屏蔽）
   │
   ├─[5] 去重（按规范化 git URL）
   │
   ├─[6] SKILL.md 探测 —— 标记哪些 repo 能装为 skill
   │
   ├─[7] 翻译（Google Translate 免费端点，per-repo 持久缓存）
   │
   ├─[8] 结构化摘要（README → 介绍 / 能干什么 / 优势 三段）
   │
   ├─[9] LLM 5 维度决策分析（可选，需配 ANTHROPIC_API_KEY 等）
   │      是什么 · 解决什么问题 · 同类项目 · 优缺点 · 何时选它
   │
   └─[10] 入库 —— Postgres 优先；失败/不可达时回退 data/latest.json
```

整个流程 ~3-5 分钟。文件锁保证两个 crawl 不会撞车。

---

## 卡片显示什么

每张卡片字段：

- `name` / `url` —— 仓库 owner/repo + GitHub 链接
- `summary_zh` —— 中文一句话介绍
- `summary_sections` —— 详细介绍（介绍 / 能干什么 / 优势，README 拆分）
- `analysis_5d` —— LLM 5 维度决策（需要配 LLM API key）
- `stars` / `forks` / `lang` / `topics`
- `trending` —— 是否今日 trending
- `local_installed` —— 你机器上是否已装
- `is_skill` —— 是否能装为 skill（前端据此显示「安装」按钮）

**重要**：不是所有 GitHub 仓库都能装为 skill —— 只有仓库根或 `/skills/` 目录下有 `SKILL.md` / `skill.md` / `SKILL.yaml` 的才算。LLM 框架、IDE 插件、模型权重等都不是可装载的 skill 格式，**装按钮会隐藏**，并在卡片里说明原因。

---

## 爬虫引擎（4 级 fallback）

> 为什么是这 4 个？引擎不在多，在精、互补。日常爬取只有 httpx 在跑，不启动浏览器。

| 层级 | 引擎 | 解决什么 | 装依赖 |
|------|------|---------|--------|
| 1 | **httpx** | 最快路径：trending 是 SSR，多数直接命中 | `pip install httpx` |
| 2 | **cloudscraper** | Cloudflare challenge 中间页 | `pip install cloudscraper` |
| 3 | **playwright_stealth** | JS 渲染 / 严格指纹检测 | `pip install playwright playwright-stealth && playwright install chromium` |
| 4 | **jina** | 本地 IP 被封时换出口（公开免费） | 无（firecrawl CLI 自带） |

**质量门**：每级结果过三层判据才算数（长度 ≥ 500 字符 / 不含 bot-check 特征 / trending 页 ≥5 个 `<article class="Box-row">`），不过门自动升级下一级。

引擎白名单在 `config.toml [orchestrator].priority` —— 不在列表里的引擎永不运行（即使装了也禁用）。想用付费的 firecrawl 替代 jina？加进 priority 数组即可。

更多 10 个停用适配器（firecrawl / crawl4ai / playwright / nodriver / crawlee / scrapy / agent_reach / trafilatura / beautifulsoup / drissionpage）能力与上述 4 级重叠，代码保留着不再调度。

---

## 加新数据源

加一个 fetcher = 三个改动，~50 行：

1. **写 fetcher** `sources/<name>.py`：
   ```python
   def fetch_<name>(max_items: int = 30) -> list:
       return [{
           "name": "owner/repo", "url": "...", "desc": "...",
           "stars": 100, "lang": "Python", "topics": ["ai"],
           "source": "<name>",
       }, ...]
   ```

2. **注册到 `radar_pkg/core.py` 的 `CATEGORIES`**：
   ```python
   {"id": "<name>", "name": "...", "desc": "...", "queries": [], "source": "<name>"}
   ```

3. **挂到 `radar_pkg/crawl.py:_crawl_inner` 的 dispatch**：
   ```python
   elif src == "<name>":
       repos = fetch_<name>(max_items=30)
   ```

后续的翻译、upsert、hot_now 自动复用。改主题分类关键词看 `core.py:CATEGORY_TOPIC_HINTS`。

---

## 故障排查

### `/api/data` 返 502

**原因**：终端/浏览器走了 HTTP 代理（如 127.0.0.1:7890），代理拦截 localhost。

**解决**：
```bash
# 终端测试绕过代理
curl --noproxy '*' http://127.0.0.1:8765/api/data
# 浏览器：用 http://127.0.0.1:8765（不走代理）而不是 localhost
```

### Vite 报 `Address already in use`

```bash
lsof -ti :8765 | xargs kill -9
lsof -ti :5174 | xargs kill -9   # 5174 是我们改过的 lodestone vite 端口
cd frontend && npm run vite-only
```

### `crawl` 一直卡在 `rate-limit hit`

**原因**：GitHub Search API 30 req/min 限流。

**解决**：等 60s 自动恢复。`./radar.py crawl` 内置退避。日志在 `data/crawl.log`。

### `gh: API rate limit exceeded for <IP>`

**原因**：`gh` 的登录 token 过期了；未认证按 IP 限流（10 次/分钟）。

**解决**（选一）：
```bash
# 方式 1：交互式重新登录
gh auth login -h github.com

# 方式 2：写到 .env（gh CLI 原生读取 GH_TOKEN）
echo 'GH_TOKEN=ghp_xxxx' >> .env
```

token 有效时搜索走 GraphQL 批量（204 个 query 约 3 分钟）；失效时降级 REST → HTML 阶梯兜底，更慢（10 分钟+）但功能不丢。

### `description` 字段是英文

HF API 对 Spaces/Models 不返回 desc。`refresh_snapshot.py` 用 repo name 合成 placeholder（"Owner/RepoName — AI / ML project"），自动翻译。

### `MCP Server` 标记而不是具体语言

MCP Registry 服务不是 GitHub repo，没 language 字段。`/api/stats` 自动过滤它，`/api/data` 卡片仍显示 "MCP Server"。

### `macOS` Python 报 `SSL: CERTIFICATE_VERIFY_FAILED`

```bash
# 方案 1：装证书
/Applications/Python\ 3.13/Install\ Certificates.command

# 方案 2：装 certifi（更省事）
pip install --upgrade certifi
```

所有 scraper 已默认 `urllib → curl` 回退，应该自动绕过。

### 卡片没显示「安装」按钮

检查 `repo.is_skill`：

```bash
curl -s http://127.0.0.1:8765/api/data | python3 -c "
import json, sys
d = json.load(sys.stdin)
for r in d['hot_now'][:5]:
    print(f\"{r['name']:<35} is_skill={r.get('is_skill')}\")
"
```

`is_skill=False` 表示仓库根没有 `SKILL.md`，按设计不显示装按钮 —— 这是正确的（不是所有 GitHub 项目都能装为 skill）。

### 想启用已停用的爬虫引擎

```bash
# 编辑 config.toml，把引擎名加回 [orchestrator].priority
# 例：想用 firecrawl 替代 jina 做云端层
priority = ["httpx", "cloudscraper", "playwright_stealth", "firecrawl"]
```

`scrapers/` 里 firecrawl / crawl4ai / playwright / nodriver / crawlee 等 10 个适配器代码已就位，装上对应 Python 包就能用。

---

## 部署到服务器

> 安装/迁移到 Linux 服务器 / Docker / systemd unit？完整步骤看 [安装说明 § 部署到服务器](安装说明.md#部署到服务器)。

简要：

1. `.env` 配 PG（如果用）
2. `RADAR_HOST=0.0.0.0` 让 serve 监听外部接口（前面挂 nginx 做 TLS）
3. `AI_RADAR_HOME=/var/lib/lodestone` 让 skill 缓存落到项目目录（多用户场景）
4. `pg8000` + 可达 PG → `./radar.py crawl` 自动建表入库
5. systemd unit 守护 `./radar.py serve 8765`

---

## 文件位置

| 你想看什么 | 路径 |
|---|---|
| 数据快照 | `data/latest.json` |
| 翻译缓存 | `data/zh_cache.json` |
| README 中文摘要缓存 | `data/readme_zh_cache.json` |
| SKILL.md 探测缓存 | `data/skill_probe_cache.json` |
| LLM 5 维度分析缓存 | `data/llm_analysis_cache.json` |
| 爬取日志 | `data/crawl.log` |
| 后端日志 | `data/serve.log` |
| 爬虫引擎 | `scrapers/<name>_scraper.py` |
| 数据源 | `sources/<name>.py` |
| 数据库层 | `db/repos.py` |
| 前端主组件 | `frontend/src/components/` |
