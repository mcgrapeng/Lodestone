# ⚡ Lodestone · 使用手册

> **唯一使用文档** — 入门 / 命令 / 三模式 / 加引擎 / FAQ / 排错都在这里。
>
> 安装卸载看 [📦 安装说明.md](安装说明.md)；项目总览看 [👋 README.md](README.md)。

## 📑 目录

| § | 章节 | 你适合看吗 | ⏱️ 时间 |
|---|---|---|---|
| § 1 | [三分钟路径（老用户）](#1-三分钟路径老用户) | 装过类似 skill | 3 分钟 |
| § 2 | [从零开始（新手）](#2-从零开始新手) | 第一次用 | 10 分钟 |
| § 3 | [速查表](#3-速查表) | 查命令 | 2 分钟 |
| § 4 | [爬虫引擎（4 精选分级）](#4-爬虫引擎4-精选分级-fallback) | 想深入 | 5 分钟 |
| § 5 | [数据源扩展](#5-数据源扩展) | 加新数据源 | 10 分钟 |
| § 6 | [FAQ + 故障排查](#6-faq--故障排查) | 出错了 | 5 分钟 |

---

# § 1 三分钟路径（老用户）

> 📌 **装好了？直接看下面四步跳过其他全部。**

## 1.1 装 Skill

```bash
cd /path/to/lodestone
./install.sh install
```

> 详见 [📦 安装说明.md](安装说明.md)。装完**完全退出再重开 Claude Code**（Cmd+Q / Alt+F4）。

## 1.2 在 Claude Code 里触发

```
/zp                      # 专属命令（推荐）
"刷一下 AI 雷达"
"看看最新 AI 项目"
"最近有什么火的 AI 项目"
```

## 1.3 启动后端 + 前端

```bash
# 终端 A
./radar.py serve 8765    # 仅 127.0.0.1

# 终端 B
cd frontend && npm run dev    # → http://localhost:5173
```

或者一条命令：`cd frontend && npm run dev`（自动起 Python + Vite）

## 1.4 访问

| 服务 | 地址 | 默认账号 |
|---|---|---|
| 🖼️ **前端 SPA** | http://localhost:5173 | — |
| 🔌 后端 API | http://127.0.0.1:8765 | — |
| 🔌 JSON /api/* | http://127.0.0.1:8792/api/data | — |

## 1.5 跑 crawl（3-5 分钟）

```bash
./radar.py crawl    # 拉 GitHub + HF + MCP + arXiv + 翻译
./radar.py today    # 终端看 Top 15
```

---

# § 2 从零开始（新手）

> 🆕 **第一次用？跟着下面 5 步走，全程约 10 分钟。**

## 2.1 准备工具（一次性）

| 工具 | 怎么检查 | 没装怎么办 |
|---|---|---|
| 🐍 Python 3.10+ | `python3 --version` | Mac: `brew install python3` · Win: [python.org](https://python.org) |
| 🤖 `gh` CLI 已认证 | `gh auth status` | [cli.github.com](https://cli.github.com) + `gh auth login` |
| 🟢 Node 18+ | `node --version` | [nodejs.org](https://nodejs.org) |
| 🤖 Claude Code | `claude --version` | [claude.com/download](https://claude.com/download) |

> 📌 网络：能访问 `github.com` + `translate.googleapis.com` + `registry.modelcontextprotocol.io` 即可。

## 2.2 装 Skill

```bash
git clone <this-repo> ~/lodestone
cd ~/lodestone
./install.sh install
```

> 💡 装完会在 `~/.claude/skills/zp/` 创建软链接。验证：`ls ~/.claude/skills/zp/SKILL.md`。

## 2.3 第一次跑

```bash
cd ~/lodestone
./radar.py crawl     # 1-3 分钟（GraphQL 批量搜索）
./radar.py today     # 终端看 Top 15
```

## 2.4 起前端

```bash
cd frontend
npm install         # 首次
npm run dev         # 自动起 vite + Python API → http://localhost:5173
```

> 浏览器打开 **http://localhost:5173** 即可看到完整仪表盘。

## 2.5 看懂生成的数据

```
data/latest.json（JSON 回退模式）:
  hot_now:  50（按 ⭐ 排序的 Top 50）
  categories: 19 分类 × ~17 平均 = 291 unique repos
  stars_today: 18 个 trending 仓库（24h 增长）
```

Postgres 模式：表 `repos` / `repo_categories` / `repo_stars_history` / `crawl_log`。

---

# § 3 速查表

> 🔍 **忘了某个命令？来这里查。**

## 3.1 在 Claude Code 里触发

```
/zp                      # 专属命令（推荐）
"刷一下 AI 雷达"
"看看最新 AI 项目"
"最近有什么火的 AI 项目"
"AI radar 启动"
```

## 3.2 CLI 命令（在 lodestone 目录里）

| 命令 | 作用 | 何时用 |
|---|---|---|
| `./radar.py crawl` | 完整爬取 + 翻译 + 入库 | 每天 / 每次新数据 |
| `./radar.py today` | 终端打印 Top 15 | 快速看一眼 |
| `./radar.py serve [port]` | 起 JSON API（默认 8765） | dev / 接入前端 |
| `cd frontend /` && `npm run dev` | 起 vite + Python 一键 | 完整 dev 流程 |
| `cd frontend && npm run build` | 打包前端到 `dist/` | 生产部署 |
| `python3 -m tests.test_config` | 跑 config 测试 | 验证引擎配置 |
| `python3 -m tests.test_scrapers` | 跑 scrapers 测试 | 验证引擎分级/质量门 |
| `python3 -m tests.test_radar` | 跑 radar 主流程测试 | 验证爬取 + 过滤 |

## 3.3 API 端点（serve 8765 时）

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/api/data` | `{hot_now, categories, fetched_at}` — 主视图 |
| GET | `/api/local` | 本机已装的 skills / commands / agents / plugins |
| GET | `/api/top?page=&size=&sort=stars\|recent\|name` | 1k+ 主流 AI 项目池 |
| GET | `/api/gain?size=&min_delta=` | 24h 增长榜（需 stars_today 数据） |
| GET | `/api/workbuddy` | 编辑精选 |
| GET | `/api/stats` | 全语言 × 主题分布（按 LANG_ALLOWLIST 过滤） |
| GET | `/api/scrapers/status` | 启用引擎可用性（默认 4 个） |
| POST | `/api/crawl` | 后台触发 crawl，文件锁忙时返 409 |
| POST | `/api/install` | 克隆 + 软链接 一个 GitHub repo 为 skill |
| POST | `/api/install-cli` | 把任意 CLI 包装为 slash command |
| POST | `/api/local/replace` | 用更强 skill 替换已装的 |
| POST | `/api/local/origin` | 给 command/agent/plugin 补 GitHub URL |

## 3.4 文件位置

| 你想看什么 | 路径 |
|---|---|
| 数据快照 | `data/latest.json` |
| 翻译缓存 | `data/zh_cache.json` |
| 爬取日志 | `data/crawl.log` |
| 爬虫引擎 | `scrapers/<name>_scraper.py` |
| URL 调度 | `scrapers/url_dispatcher.py` |
| 数据源 | `sources/<name>.py` |
| 数据库层 | `db/__init__.py` / `db/repos.py` |
| 前端主组件 | `frontend/src/App.tsx` |
| 前端配置 | `frontend/vite.config.ts` |

## 3.5 更新 / 卸载

```bash
# 更新 skill（修改源后）
cd ~/lodestone
git pull
# 重启 Claude Code（让 SKILL.md 重新加载）

# 卸载 skill
cd ~/lodestone && ./uninstall.sh

# 删源（可选）
rm -rf ~/lodestone

# 删生成的数据（可选）
rm -rf ~/lodestone/data/latest.json ~/lodestone/data/zh_cache.json
```

---

# § 4 爬虫引擎（4 精选，分级 fallback）

> 🔍 **为什么从 14 个精简到 4 个？** 引擎不在多，在精、互补、开源、主流。
> 4 级各解决一种别的级解决不了的失败模式，日常爬取只有 httpx 在跑，不启动浏览器。

## 4.1 分级阶梯（轻 → 重 → 云）

| 层级 | 引擎 | 解决的失败模式 | 装依赖 |
|------|------|---------------|-------|
| 1 · 本地纯 HTTP | httpx | 最快路径 — trending 是 SSR，多数直接命中 | `pip install httpx` |
| 2 · 本地挑战破解 | cloudscraper | bot-check 中间页（Cloudflare challenge） | `pip install cloudscraper` |
| 3 · 本地真浏览器 | playwright_stealth | JS 渲染 / 严格指纹检测 | `pip install playwright playwright-stealth && playwright install chromium` |
| 4 · 云端渲染 | jina | 本地 IP 被封时逃生（r.jina.ai 免费层） | 无（公开 endpoint） |

**质量门**：每级结果须通过三层判据才算数 —
① 长度 ≥ `min_html_length`；② 不含 bot-check 特征（Just a moment 等）；
③ 已知页面的结构标记足够（trending 须 ≥5 个 `<article class="Box-row"`，
search 须 ≥3 个 `data-view-component`，与 radar.py 解析正则耦合）。
不过门 → 自动升级下一级引擎。

其余 10 个适配器（firecrawl / crawl4ai / playwright / nodriver / crawlee /
scrapy / agent_reach / trafilatura / beautifulsoup / drissionpage）能力与
上述 4 级重叠，已停用但代码保留 — 想重启见 § 4.3。

## 4.2 URL 调度策略

`scrapers/url_dispatcher.py` 按 host 模式给出该 URL 家族的引擎顺序（轻→重）：

```python
URL_PATTERNS = [
    ("github.com",        ["httpx", "cloudscraper", "playwright_stealth", "jina"]),
    ("huggingface.co",    ["httpx", "jina"]),
    ("github.io",         ["httpx", "jina"]),
    ("pypi.org",          ["httpx", "jina"]),
    ...
    ("_default",          ["httpx", "cloudscraper", "playwright_stealth", "jina"]),
]
```

默认编排 `tiered` 分级 fallback；`selection_strategy = "longest"` 切回旧的
全引擎并行竞赛（最长 HTML 胜出，调试用）。并行模式对 markdown-hint URL
（HF / arxiv 等内容站）仍优先返回 jina 的 markdown。

## 4.3 配置引擎

```toml
# config.toml
[orchestrator]
wall_clock_timeout = 60          # 分级阶梯共享预算（秒）
selection_strategy = "tiered"    # "tiered" | "longest" | "first"
priority = [                     # 唯一白名单 + 升级顺序（轻→重）
    "httpx", "cloudscraper", "playwright_stealth", "jina",
]

[timeouts]                       # 覆盖全部 14 个适配器（停用的可随时重启）
httpx = 60
cloudscraper = 90
...
```

**重启已停用的引擎**：把引擎名加回 `priority` 数组即可（如想用付费的
firecrawl 替代 jina 做云端层）。列表外的引擎永远不会运行。

## 4.4 验证哪些引擎可用

```bash
python3 -c "
import sys; sys.path.insert(0, '.')
from scrapers import status
s = status()
print(f'{sum(1 for v in s.values() if v)}/{len(s)} engines available')
"
```

---

# § 5 数据源扩展

> 🔍 **想加新数据源？编辑 `sources/` 目录。**

## 5.1 数据源目录结构

```
sources/
├── __init__.py
├── mcp_registry.py            # MCP Server Registry fetcher
├── huggingface_models.py      # HF Models Trending fetcher
├── arxiv_papers.py            # arXiv 最新 AI 论文（官方 Atom API）
└── github_graphql.py          # GitHub GraphQL 批量搜索（N 个 query / 请求）
```

每个 fetcher 必须返回 `list[dict]`，每条记录有 `name` / `url` / `stars` / `lang` / `topics` / `desc` 等标准字段。

## 5.2 添加新数据源的步骤

1. **写 fetcher** `sources/<name>.py`：
   ```python
   def fetch_<name>(max_items: int = 30) -> list:
       # 调用 API 或爬取
       return [{
           "name": "owner/repo",
           "url": "https://...",
           "desc": "...",
           "stars": 100,
           "lang": "Python",
           "topics": ["ai"],
           "source": "<name>",
       }, ...]
   ```

2. **添加到 `radar.py CATEGORIES`**：
   ```python
   {"id": "<name>", "name": "...", "desc": "...", "queries": [], "source": "<name>"},
   ```

3. **添加到 `_crawl_inner` dispatch**：
   ```python
   elif src == "<name>":
       repos = fetch_<name>(max_items=30)
   ```

4. **测试**：
   ```bash
   python3 -c "from sources.<name> import fetch_<name>; print(fetch_<name>(max_items=3))"
   ```

## 5.3 修改主题分类关键词

`refresh_snapshot.py` 顶部 `CATEGORY_TOPIC_HINTS` 定义 5k+ 池 repo 的 `best_category` 推断。改这里即可。

## 5.4 增量更新数据（不跑全 crawl）

`refresh_snapshot.py` 拉取 4 个非 GitHub 数据源 + 翻译 + 写 `data/latest.json`，**不调用任何 GitHub API**。约 30 秒：

```bash
python3 refresh_snapshot.py
```

---

# § 6 FAQ + 故障排查

## 6.1 FAQ

### Q：四个数据源覆盖所有主流 AI 工具了吗？

A：**4 个数据源（GitHub + HF Spaces + HF Models + MCP Registry）共 327+ repos**。GitHub 是主流开源主战场；HF Spaces 是社区 demo；HF Models 是模型权重；MCP Registry 是 MCP 协议生态。任何 4 个都单独无法覆盖所有 — 建议加上 `refresh_snapshot.py` 跑增量 + 每周 `crawl` 跑全量。

### Q：1k+ 主流 AI 项目池包含什么？

A：`/api/top?size=N` 返回全部 327+ 个，按 ⭐ 排序。**没有 ⭐ 阈值过滤**（2026-08 移除 stars>=1000 gate）。可通过 size=12 默认页大小分页。

### Q：今日星增榜怎么用？

A：`/api/gain?size=N&min_delta=M`。`stars_today` 字段从 github.com/trending 抓取，每个 trending repo 都被 gh API 二次 enrich（lang / topics / desc_zh）。需要连续多天 crawl 才有完整数据。

### Q：中文详解是 AI 总结还是字面翻译？

A：**字面翻译**（Google Translate 免费 endpoint）。`desc_zh` 是 `desc` 的中文翻译；`summary_zh` 是 `desc_zh` 的首句（≤ 80 字符）。**没有 AI 总结**（未接 LLM）。

### Q：安装 CLI 默认勾选哪些？

A：**默认全部不勾选**（2026-08 改）。用户必须主动选要安装到哪些 CLI（`claude` / `codex` / `opencode`）。这是安全设计 — 自动装到所有 CLI 可能让用户误装。

### Q：Jupyter notebook 算语言吗？

A：**不算**。`LANG_ALLOWLIST`（57 个编程语言）不含 `Jupyter Notebook`。`/` 列被合并为 "unknown"。

### Q：怎么只跑 MCP / HF 源（不依赖 GitHub）？

A：`python3 refresh_snapshot.py` — 只拉 MCP Registry + HF Spaces + HF Models + 翻译 + enrich trending，**完全绕开 GitHub**。

### Q：忘了 admin 密码？

N/A — Lodestone 无 admin 密码概念。`data/` 是普通文件，所有操作不需要鉴权。

### Q：能给卡片加 AI 摘要吗？

需要接 LLM（Claude / GPT）。`refresh_snapshot.py` 加 `_llm_summarize()` 模块，把 `summary_zh` 字段从字面首句升级为 LLM 生成的 1-2 句摘要。

## 6.2 故障排查

### Q：`/api/crawl` 返 502

**原因**：curl/浏览器走 HTTP_PROXY（127.0.0.1:7890），代理拦截 localhost。

**解决**：
- 终端测试：`curl --noproxy '*' http://127.0.0.1:5173/api/stats`
- 浏览器：**直接用 `http://localhost:5173`**（不经过代理）— vite 默认绑 IPv6，访问时用 `localhost` 通常能解析到 IPv4

### Q：vite 启动失败：`Address already in use`

**原因**：8765 已被之前的 serve 占用。

**解决**：
```bash
# 杀掉占用的 Python radar.py serve
lsof -ti :8765 | xargs kill -9
# 重启 dev.cjs
cd frontend && node dev.cjs
```

### Q：crawl 跑很久没进度（GitHub 二次限流）

**原因**：GitHub Search API 30 req/min 限制。

**解决**：等 60s 自动恢复（`_SEARCH_PACE` 退避机制）。日志看 `data/crawl.log` 进度。

### Q：`description 字段是英文`

**原因**：HF API 对 Spaces/Models 不返回 description（前端显示 "原文" 标签）。

**解决**：`refresh_snapshot.py` 用 repo name 合成 placeholder desc（"Owner/RepoName — AI / ML project"），并自动翻译。

### Q：MCP servers 标记 "MCP Server" 而不是具体语言

**原因**：MCP Registry 服务不是 GitHub repo，没有 language 字段。

**解决**：`LANG_ALLOWLIST` 不包含 "MCP Server" — 它在 `/api/stats` 被自动过滤。`/api/data` 的 card 上仍显示 "MCP Server" 作为 lang。

### Q：未知语言 (`unknown`) 还剩  多个

**原因**：markdown 文档类 repo（如 awesome-lists）或 trending 抓的不完整元数据。

**解决**：`refresh_snapshot.py` 用 gh API 二次 enrich trending scraped repos（已实装）。剩下 15 个真实无语言。

### Q：`pip install` 装包失败

```bash
# macOS Python SSL 证书
/Applications/Python\ 3.13/Install\ Certificates.command

# 系统级 pip
pip3 install --user <pkg>
```

### Q：装好了引擎但 `is_available=False`，或想用停用的引擎

`is_available=False` = 对应 Python 包没装。注意 2026-09 起只有 `priority`
白名单里的 4 个引擎会运行（httpx / cloudscraper / playwright_stealth / jina），
装了其他引擎也不会被调度 — 想启用就把引擎名加回 `config.toml` 的 `priority`。

```bash
# 检查模块是否真装了
python3 -c "import playwright_stealth; print('ok')"
python3 -c "import cloudscraper; print('ok')"
python3 -c "import httpx; print('ok')"
# 缺哪个装哪个（4 个精选引擎的完整依赖见 § 4.1）
pip3 install httpx cloudscraper playwright-stealth
```

### Q：macOS Python  3.13 报 `SSL: CERTIFICATE_VERIFY_FAILED`

```bash
# 安装 certifi 或用 curl 回退（已默认在大部分 scraper 里实现）
pip3 install --upgrade certifi
```

### Q：`gh: API rate limit exceeded for <IP>` / GraphQL 批量失败

`gh` 的登录 token 过期了（未认证请求按 IP 限流，10 次/分钟）。两种修法：

```bash
# 方式一：重新登录（交互式）
gh auth login -h github.com

# 方式二：token 写进 .env（非交互；gh 原生读取 GH_TOKEN 环境变量）
echo 'GH_TOKEN=ghp_xxxx' >> .env
```

token 有效时搜索走 GraphQL 批量（分批并行 + 失败批对半拆分重试，204 个 query
约 3 分钟跑完）；token 失效时自动降级 REST → HTML 阶梯兜底，功能不丢但更慢
（单次 crawl 可能 10 分钟+）。

---

**祝你玩得开心！** 🎉