{
  "name": "lodestone",
  "version": "0.1.0",
  "private": true,
  "description": "Root shim — actual UI lives in frontend/. Use `npm run dev` from here OR `cd frontend && npm run dev`.",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  }
}
