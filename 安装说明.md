# 📦 Lodestone · 安装说明

> **目标**：把 `/刷一下 AI 雷达` 装到 Claude Code（双 CLI 兼容），从 0 到能跑通 `radar.py crawl`。
>
> 本文档**只讲安装 / 卸载 / 更新**。所有用法看 [📖 使用手册.md](使用手册.md)。

## 📑 目录

1. [系统要求](#1-系统要求)
2. [一键安装](#2-一键安装)
3. [高级选项](#3-高级选项)
4. [验证安装](#4-验证安装)
5. [更新](#5-更新)
6. [卸载](#6-卸载)
7. [常见安装问题](#7-常见安装问题)

---

## 1. 系统要求

| 工具 | 最低版本 | 检查命令 | 没装怎么办 |
|---|---|---|---|
| 🐍 **Python** | 3.10+ | `python3 --version` | Mac: `brew install python3` · Win: [python.org](https://python.org) |
| 🤖 **`gh` CLI 已认证** | v2.0+ | `gh auth status` | [cli.github.com](https://cli.github.com) + `gh auth login` |
| 🟢 **Node.js** | 18+ | `node --version` | [nodejs.org](https://nodejs.org) |
| 🤖 **Claude Code** | v1.0+ | `claude --version` | [claude.com/download](https://claude.com/download) |
| 🌿 **Git** | 任意 | `git --version` | Mac: `brew install git` · Win: [git-scm.com](https://git-scm.com) |
| 🌐 **网络** | 可达 github.com + translate.googleapis.com + registry.modelcontextprotocol.io | 浏览器试一下 | 公司内网找 IT 代理 |

> 💡 **磁盘占用**：约 50 MB（工程源）+ `frontend/node_modules/` 约 200 MB + 可选爬虫依赖（`playwright install chromium` 约 300 MB）。

> ⚠️ **macOS 用户**：系统 Python SSL 证书可能缺失。运行 `/Applications/Python\ 3.13/Install\ Certificates.command` 修复，或依赖默认的 curl 回退。

---

## 2. 一键安装

> ⏱️ 全程约 **3 分钟**。

### 2.1 方式 A · Git clone（推荐）

```bash
# 1. 拉工程
git clone <this-repo> ~/lodestone
cd ~/lodestone

# 2. 一键装到 Claude Code + Codex CLI
./install.sh install

# 3. 完全退出 Claude Code（Cmd+Q / Alt+F4）再重开
```

> 💡 装完会在 `~/.claude/skills/zp/` 和 `~/.codex/skills/zp/` 创建软链接。

### 2.2 方式 B · 下载 ZIP

> 不知道怎么用 git？也 OK：

1. GitHub 页面点 `Code` → `Download ZIP`
2. 解压到任意位置（如 `~/Documents/lodestone`）
3. 终端 cd 进去跑 `./install.sh install`

### 2.3 安装过程做什么？

`install.sh install` 自动完成：

1. ✅ 验证源目录（含 `SKILL.md`）
2. ✅ 创建 `~/.claude/skills/` 和 `~/.codex/skills/`
3. ✅ 软链接工程到两个 skill 目录（**修改源即时生效**）

```
⚡ Lodestone · GitHub + HuggingFace 双源 AI 工具发现平台
   源目录: /Users/you/lodestone

📦 Claude Code (~/.claude/skills/zp)
  ✓ symlinked

📦 Codex CLI (~/.codex/skills/zp)
  ✓ symlinked

🎉 安装完成！
```

---

## 3. 高级选项

```bash
# 看所有选项
./install.sh help
```

`install.sh` 接受以下子命令：

| 子命令 | 作用 |
|---|---|
| `install` | 装 skill 到 `~/.claude/skills/` 和 `~/.codex/skills/`（符号链接） |
| `uninstall` | 删两个 skill 软链接 |

---

## 4. 验证安装

### 4.1 一键验证脚本

```bash
./install.sh status
```

期望输出：

```
== Lodestone skill 安装状态 ==

  源目录：    /Users/you/lodestone
  目标目录：  /Users/you/.claude/skills

  [zp]
    状态：    已安装（符号链接）
    SKILL.md：✅

  ✅ Claude Code 软链接正常
```

### 4.2 跑一次真命令

打开 Claude Code，输入：

```
"刷一下 AI 雷达"
```

或直接验证雷达：

```bash
cd ~/lodestone
./radar.py today   # 应打印 Top 15
```

### 4.3 两个 CLI 都在吗？

```bash
ls ~/.claude/skills/zp/SKILL.md   # Claude Code → 必须存在
ls ~/.codex/skills/zp/SKILL.md   # Codex CLI → 必须存在
```

---

## 5. 更新

### 5.1 符号链接模式（默认）

> **无需任何操作！** 改源仓库后，所有安装项目**自动反映**新内容（下次 Claude Code 启动时读取 SKILL.md，需重启）。

```bash
cd ~/lodestone
git pull
# 重启 Claude Code（让 SKILL.md 重新加载）
```

### 5.2 复制模式

不适用——本工程只支持符号链接模式（`install.sh` 内置）。改源后自动同步。

### 5.3 更新会破坏已生成的数据吗？

> **不会！** 数据快照在 `data/latest.json`，独立于 skill。`install.sh` 只刷新 skill目录，不动数据。

要更新数据快照，跑：

```bash
./radar.py crawl              # 完整爬取
# 或：
python3 refresh_snapshot.py  # 只刷新 MCP / HF / 翻译
```

---

## 6. 卸载

### 6.1 卸载 skill

```bash
cd ~/lodestone
./uninstall.sh
```

输出：

```
== 卸载 Lodestone Skill ==
  ✅  已删除 ~/.claude/skills/zp
  ✅  已删除 ~/.codex/skills/zp
```

### 6.2 删源（可选）

```bash
rm -rf ~/lodestone
```

### 6.3 删数据快照（可选）

```bash
rm -rf ~/lodestone/data/latest.json \
       ~/lodestone/data/zh_cache.json
```

### 6.4 清爬虫依赖（可选）

```bash
pip3 uninstall playwright playwright-stealth crawl4ai nodriver \
                crawlee scrapy cloudscraper agent_reach httpx \
                trafilatura beautifulsoup4 DrissionPage firecrawl
```

---

## 7. 常见安装问题

### Q1：`python3: command not found`

**原因**：Python 没装或没加 PATH。

**解决**：
- Mac: `brew install python3` 或 [python.org](https://python.org)
- Linux: `sudo apt install python3.10`
- 用 `python3` 代替 `python`（macOS 自带）

### Q2：`gh: command not found`

**解决**：[cli.github.com](https://cli.github.com) 装 `gh` CLI，然后 `gh auth login`。

### Q3：`gh auth status` 失败 / 报 401

**原因**：未认证或 token 过期。

**解决**：`gh auth login`（按提示选 GitHub.com + HTTPS + 浏览器认证）。

### Q4：`node: command not found`

**解决**：[nodejs.org](https://nodejs.org) 装 Node 18+。

### Q5：`./install.sh install` 报 "缺少 SKILL.md"

**原因**：当前目录不是有效的 lodestone 仓库。

**解决**：

```bash
# 确认当前在仓库根目录
ls SKILL.md   # 必须存在
# 不存在 → 重新 git clone
```

### Q6：`./install.sh install` 报 "目标已存在"

**原因**：`~/.claude/skills/zp` 等已存在（可能装过一次）。

**解决**：按提示输入 `y` 替换，或手动：

```bash
rm -rf ~/.claude/skills/zp \
       ~/.codex/skills/zp
./install.sh install
```

### Q7：装好了但 Claude Code 不识别 `/刷一下 AI 雷达`

**解决**：

1. **完全退出 Claude Code**（Cmd+Q / Alt+F4）
2. 重新打开
3. 直接说"刷一下 AI 雷达"
4. 还不行：删 skill 重装 → `./install.sh uninstall && ./install.sh install`

### Q8：crawl 跑出 `SSL: CERTIFICATE_VERIFY_FAILED`

**原因**：macOS Python 系统证书缺失。

**解决**：

```bash
# 选项 1：装 certificates
/Applications/Python\ 3.13/Install\ Certificates.command

# 选项 2：所有 scraper 已默认 urllib → curl 回退，应该自动绕过
# 选项 3：设置 PYTHONHTTPSVERIFY=0（不推荐生产）
```

### Q9：`./radar.py crawl` 一直卡在 `rate-limit hit`

**原因**：GitHub Search API 二次限流（30 req/min）。

**解决**：等 60s 自动退避。日志在 `data/crawl.log`。

### Q10：可以装在 WSL / Linux 服务器吗？

> 可以。Lodestone 是纯 Python + Node，跨平台：

```bash
# Ubuntu 22.04+
sudo apt update
sudo apt install -y python3.10 python3-pip nodejs npm

# 装 skill
cd ~/lodestone
./install.sh install
```

### Q11：可以多台机器共享同一个 lodestone 仓库吗？

> 可以。每台机器单独 `./install.sh install`，共享 Git 仓库：

```bash
# 机器 A
git clone <repo> ~/lodestone
./install.sh install

# 机器 B
git clone <repo> ~/lodestone
./install.sh install
```

---

## ✅ 安装完成的标志

- ✅ `./install.sh status` 输出 `zp ✅`
- ✅ `~/.claude/skills/zp/SKILL.md` 文件存在
- ✅ `~/.codex/skills/zp/SKILL.md` 文件存在（双 CLI 兼容）
- ✅ Claude Code 里说"刷一下 AI 雷达"能触发
- ✅ `./radar.py today` 打印 Top 15

**下一步**：看 [📖 使用手册.md](使用手册.md) § 1 跑通第一次 crawl。

---

## 📚 相关文档

| 你想看什么 | 跳到 |
|---|---|
| 👋 项目总览 | [README.md](README.md) |
| 🚀 极速上手（3 分钟） | [📖 使用手册.md](使用手册.md) § 1 |
| 🆕 完全新手版 | [📖 使用手册.md](使用手册.md) § 2 |
| 🛠️ 使用手册 + FAQ | [📖 使用手册.md](使用手册.md) |
| 🤖 Skill 触发入口 | [SKILL.md](SKILL.md) |

---

**版本**：v1.0 · 2026-08-25