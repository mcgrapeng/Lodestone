# 安装说明

把 Lodestone 装好、跑起来。

## 你需要什么

| 工具 | 版本 | 没装怎么办 |
|---|---|---|
| Python | 3.10+ | `brew install python3` / `apt install python3.10` |
| Node.js | 18+ | [nodejs.org](https://nodejs.org) |
| Git | 任意 | 系统自带，或 `brew install git` |
| `gh` CLI + 已登录 | 2.0+ | [cli.github.com](https://cli.github.com) → `gh auth login` |

网络能访问 `github.com` + `translate.googleapis.com` + `registry.modelcontextprotocol.io` 就行。

> 想用 Claude Code 里的 `/刷一下 AI 雷达` 入口？再装个 [Claude Code](https://claude.com/download)。可后装，先把后端跑起来。

## 三步装好

```bash
# 1. 拉代码
git clone <仓库地址> ~/lodestone
cd ~/lodestone

# 2. 装依赖（Python 端零依赖；Node 端进 frontend/ 跑 npm install）
pip install pg8000         # 可选 — 接 Postgres 时需要
(cd frontend && npm install --no-audit --no-fund)

# 3. 跑一次，生成数据快照
./radar.py crawl
./radar.py today            # 终端看 Top 15，能打印就说明装好了
```

> 想要 Claude Code 一键入口？装 skill（详见下文 [§ 装 skill](#装-skill)）。

### 装 skill（可选）

把 `/刷一下 AI 雷达` 这个命令装到 Claude Code / Codex：

```bash
./install.sh install    # 软链接到 ~/.claude/skills/ 和 ~/.codex/skills/
./install.sh status     # 验证
```

装完**完全退出 Claude Code 再重开**（Cmd+Q / Alt+F4）让它加载 SKILL.md。

## 部署到服务器

> 本地能跑 ≠ 服务器能跑。下面三件事必须改，否则会在 Linux 服务器上炸。

### 1. `.env` 配 Postgres（推荐）

```bash
cp .env.example .env
# 编辑 .env，把 PGHOST/PGUSER/PGPASSWORD/PGDATABASE 改成服务器实际值
```

`.env` 里的所有变量在每个 radar.py 命令开头自动加载。Shell 环境变量优先级更高。

### 2. 让 serve 监听外部接口

默认 `serve` 只绑 `127.0.0.1`（安全：API 能 `git clone` 你的 skill 目录，不暴露到内网）。

服务器需要别人访问时：

```bash
# .env 里加
RADAR_HOST=0.0.0.0

# 同时强烈建议前面挂 nginx / caddy 做 TLS + 鉴权
```

### 3. 把 skill 缓存重定向到项目目录

默认 skill 缓存写 `$HOME/.cache/lodestone/`，多用户服务器需要重定向：

```bash
# .env 里加
AI_RADAR_HOME=/var/lib/lodestone
mkdir -p /var/lib/lodestone
```

数据库 schema（`repos` / `repo_categories` / `repo_stars_history` / `crawl_log`）由 `db.ensure_schema()` 在第一次 crawl 时自动建。装好 `.env` + PG 可达，直接 `./radar.py crawl` 就行。

## 验证安装

```bash
./install.sh status                # 看到 "zp ✅" = skill 装好了
ls ~/.claude/skills/zp/SKILL.md    # Claude Code 入口文件存在
./radar.py today                   # 终端有 Top 15 输出 = 后端通了
./radar.py serve 8765 &
curl http://127.0.0.1:8765/api/data | head -c 200    # API 返回 JSON = 数据层通了
```

四个检查全过 = 装好了。

## 更新

```bash
cd ~/lodestone
git pull
# skill 是符号链接 → 自动同步新代码
# 重启 Claude Code（让 SKILL.md 重新加载）
```

数据快照独立于代码：`./radar.py crawl` 重新生成即可，旧 `data/latest.json` 不会被破坏。

## 卸载

```bash
./uninstall.sh               # 删两个 skill 软链接
rm -rf ~/lodestone            # 删源码（可选）
rm -rf ~/lodestone/data/*     # 清数据快照（可选）
```

## 故障排查

| 报错 | 原因 | 解决 |
|---|---|---|
| `python3: command not found` | Python 没装或不在 PATH | 装 Python 3.10+ |
| `gh: command not found` / `gh auth status` 失败 | `gh` CLI 未装 / 未登录 | 装 `gh` → `gh auth login` |
| `node: command not found` | Node 没装 | 装 Node 18+ |
| `install.sh` 报 "缺少 SKILL.md" | 当前不在仓库根 | `cd ~/lodestone` 重试 |
| Claude Code 不识别 `/刷一下 AI 雷达` | SKILL.md 没加载 | 完全退出 Claude Code（Cmd+Q）再重开；不行就 `uninstall.sh && install.sh` |
| `crawl` 报 `SSL: CERTIFICATE_VERIFY_FAILED` | macOS Python 缺证书链 | `/Applications/Python\ 3.13/Install\ Certificates.command`；或装 `certifi` |
| `crawl` 一直卡在 `rate-limit hit` | GitHub Search API 30 req/min 限流 | 等 60s 自动恢复，crawl 内置退避 |

## 下一步

- 怎么用 → [使用手册](使用手册.md)
- 这是什么 → [README](README.md)
- skill 触发入口 → [SKILL.md](SKILL.md)
