'use client';
import { jsx } from 'react/jsx-runtime';
import * as React from 'react';
import { cn } from '../../internal/utils.js';
import { useReducedMotion } from '../../hooks/use-reduced-motion.js';
import { PatternLayer, patternSpotlight } from './background-pattern-shared.js';

const SPOTLIGHT = "radial-gradient(circle var(--pattern-highlight) at var(--pattern-x, 50%) var(--pattern-y, 50%), #000 0%, #000 5%, 35%, transparent 100%)";
const FADE_DURATION = 1200;
function BackgroundPatternInteractive({
  variant,
  persistent: persistentProp = false,
  track = "self"
}) {
  const ref = React.useRef(null);
  const reducedMotion = useReducedMotion();
  const persistent = persistentProp || reducedMotion;
  React.useEffect(() => {
    const el = ref.current;
    const host = el?.parentElement;
    if (!el || !host) return;
    const target = track === "window" ? window : host;
    let frame = 0;
    let clientX = 0;
    let clientY = 0;
    const flush = () => {
      frame = 0;
      const rect = host.getBoundingClientRect();
      el.style.setProperty("--pattern-x", `${clientX - rect.left}px`);
      el.style.setProperty("--pattern-y", `${clientY - rect.top}px`);
    };
    const fade = !persistent && typeof el.animate === "function" ? el.animate([{ opacity: 1 }, { opacity: 0 }], {
      duration: FADE_DURATION,
      easing: "ease-out",
      fill: "forwards"
    }) : null;
    fade?.finish();
    const handleMove = (event) => {
      clientX = event.clientX;
      clientY = event.clientY;
      if (!frame) frame = requestAnimationFrame(flush);
      if (fade) {
        fade.currentTime = 0;
        fade.play();
      }
    };
    target.addEventListener("pointermove", handleMove, { passive: true });
    return () => {
      if (frame) cancelAnimationFrame(frame);
      fade?.cancel();
      target.removeEventListener("pointermove", handleMove);
    };
  }, [persistent, track]);
  return /* @__PURE__ */ jsx(
    "div",
    {
      ref,
      "aria-hidden": true,
      "data-slot": "background-pattern-highlight",
      "data-persistent": persistent ? "" : void 0,
      className: cn(
        "pointer-events-none absolute inset-0 -z-10 text-(--pattern-color)",
        persistent ? "opacity-100" : "opacity-0"
      ),
      style: { WebkitMaskImage: SPOTLIGHT, maskImage: SPOTLIGHT },
      children: /* @__PURE__ */ jsx(PatternLayer, { variant, className: patternSpotlight(variant) })
    }
  );
}

export { BackgroundPatternInteractive };
//# sourceMappingURL=background-pattern-interactive.js.map
