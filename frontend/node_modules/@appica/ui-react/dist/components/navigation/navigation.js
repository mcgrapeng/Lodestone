'use client';
import { jsx } from 'react/jsx-runtime';
import * as React from 'react';
import { cn } from '../../internal/utils.js';
import { NavigationContext, useNavigationContext } from './navigation-context.js';

function Navigation({
  className,
  orientation = "horizontal",
  variant = "pill",
  size = "md",
  activeLink = null,
  ...props
}) {
  const ctx = React.useMemo(
    () => ({ orientation, variant, size, activeLink }),
    [orientation, variant, size, activeLink]
  );
  return /* @__PURE__ */ jsx(NavigationContext.Provider, { value: ctx, children: /* @__PURE__ */ jsx("nav", { "data-slot": "navigation", "data-orientation": orientation, className, ...props }) });
}
const HORIZONTAL_GAP = {
  pill: "gap-0.5",
  line: "gap-7"
};
const VERTICAL_GAP = {
  pill: "gap-0.5"
};
function NavigationList({ className, ...props }) {
  const ctx = useNavigationContext();
  const orientation = ctx?.orientation ?? "horizontal";
  const variant = ctx?.variant ?? "pill";
  const gap = (orientation === "vertical" ? VERTICAL_GAP : HORIZONTAL_GAP)[variant];
  const layout = cn("flex", orientation === "vertical" && "flex-col", gap);
  return /* @__PURE__ */ jsx("ul", { "data-slot": "navigation-list", role: "list", className: cn(layout, className), ...props });
}
function NavigationItem({ className, ...props }) {
  return /* @__PURE__ */ jsx("li", { "data-slot": "navigation-item", className, ...props });
}

export { Navigation, NavigationItem, NavigationList };
//# sourceMappingURL=navigation.js.map
