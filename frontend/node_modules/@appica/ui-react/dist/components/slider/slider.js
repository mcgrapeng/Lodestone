'use client';
import { jsx, jsxs } from 'react/jsx-runtime';
import * as React from 'react';
import { Slider as Slider$1 } from '@base-ui/react/slider';
import { LazyMotion, domAnimation, useAnimate, AnimatePresence, m } from 'motion/react';
import { cn } from '../../internal/utils.js';
import { useReducedMotion } from '../../hooks/use-reduced-motion.js';
import { useDirection } from '@base-ui/react/direction-provider';

const TOOLTIP_TILT_DEG = 8;
const TOOLTIP_TILT_TRANSITION = {
  duration: 0.4,
  ease: [0.34, 1.56, 0.64, 1]
};
const TOOLTIP_POPUP_TRANSITION = {
  duration: 0.2,
  ease: [0.175, 0.885, 0.32, 1.5]
};
const tooltipWrapperClasses = "absolute z-10 pointer-events-none transition-transform duration-250 group-hover:scale-110 group-focus-within:scale-110 motion-reduce:transition-none data-[orientation=horizontal]:bottom-full data-[orientation=horizontal]:left-1/2 data-[orientation=horizontal]:-translate-x-1/2 data-[orientation=horizontal]:mb-1 data-[orientation=horizontal]:origin-bottom data-[orientation=vertical]:top-1/2 data-[orientation=vertical]:-translate-y-1/2 data-[orientation=vertical]:inset-s-full data-[orientation=vertical]:ms-1 data-[orientation=vertical]:ltr:origin-left data-[orientation=vertical]:rtl:origin-right";
const tooltipInnerClasses = "bg-background-inverse text-foreground-inverse rounded-4xs block px-1 py-0.5 text-xs leading-none whitespace-nowrap data-[orientation=horizontal]:origin-bottom data-[orientation=vertical]:ltr:origin-left data-[orientation=vertical]:rtl:origin-right";
function SliderTooltip({
  index,
  visibility,
  orientation,
  changeVersion,
  direction,
  dirSign,
  reduced,
  open
}) {
  const [scope, animate] = useAnimate();
  React.useEffect(() => {
    if (reduced || changeVersion === 0 || !scope.current) return;
    const sign = direction === "up" ? 1 : -1;
    const tilt = -sign * dirSign * TOOLTIP_TILT_DEG;
    animate(scope.current, { rotate: [tilt, 0] }, TOOLTIP_TILT_TRANSITION);
  }, [changeVersion, direction, dirSign, reduced, animate, scope]);
  const visible = visibility === "always" || open;
  return /* @__PURE__ */ jsx("span", { "data-slot": "slider-tooltip-wrapper", "data-orientation": orientation, className: tooltipWrapperClasses, children: /* @__PURE__ */ jsx(AnimatePresence, { children: visible && /* @__PURE__ */ jsx(
    m.div,
    {
      ref: scope,
      "data-slot": "slider-tooltip",
      "data-orientation": orientation,
      "aria-hidden": "true",
      className: tooltipInnerClasses,
      initial: reduced ? false : { opacity: 0, scale: 0.9 },
      animate: { opacity: 1, scale: 1 },
      exit: reduced ? { opacity: 0 } : { opacity: 0, scale: 0.95 },
      transition: reduced ? { duration: 0 } : TOOLTIP_POPUP_TRANSITION,
      children: /* @__PURE__ */ jsx(Slider$1.Value, { children: (formattedValues) => /* @__PURE__ */ jsx("span", { className: "leading-none", children: formattedValues?.[index] ?? "" }) })
    }
  ) }) });
}
const thumbClasses = "group relative block size-3 shrink-0 cursor-default rounded-full select-none disabled:pointer-events-none disabled:opacity-disabled";
const thumbVisualClasses = "bg-background border-primary relative block size-full rounded-full border-2 shadow-xs transition-transform duration-250 group-hover:scale-125 group-focus-within:scale-125 motion-reduce:transition-none";
function SliderThumb({
  index,
  ariaLabel,
  tooltipVisibility,
  orientation,
  changeVersion,
  direction,
  dirSign,
  reduced
}) {
  const [hovered, setHovered] = React.useState(false);
  const [focused, setFocused] = React.useState(false);
  const autoOpen = tooltipVisibility === "auto" && (hovered || focused);
  const eventHandlers = tooltipVisibility === "auto" ? {
    onPointerEnter: () => setHovered(true),
    onPointerLeave: () => setHovered(false),
    onFocus: () => setFocused(true),
    onBlur: () => setFocused(false)
  } : {};
  return /* @__PURE__ */ jsxs(
    Slider$1.Thumb,
    {
      index,
      "aria-label": ariaLabel,
      "data-slot": "slider-thumb",
      className: thumbClasses,
      ...eventHandlers,
      children: [
        /* @__PURE__ */ jsx("span", { "data-slot": "slider-thumb-visual", className: thumbVisualClasses }),
        tooltipVisibility !== "never" && /* @__PURE__ */ jsx(
          SliderTooltip,
          {
            index,
            visibility: tooltipVisibility,
            orientation,
            changeVersion,
            direction,
            dirSign,
            reduced,
            open: autoOpen
          }
        )
      ]
    }
  );
}
const rootClasses = "data-[orientation=horizontal]:w-full data-[orientation=vertical]:h-full data-[orientation=vertical]:w-fit";
const controlClasses = "relative flex w-full touch-none items-center select-none data-disabled:opacity-disabled data-disabled:pointer-events-none data-[orientation=vertical]:h-full data-[orientation=vertical]:min-h-40 data-[orientation=vertical]:w-auto data-[orientation=vertical]:flex-col";
const trackClasses = "relative grow overflow-hidden rounded-full bg-background-strong select-none data-[orientation=horizontal]:h-1.5 data-[orientation=horizontal]:w-full data-[orientation=vertical]:h-full data-[orientation=vertical]:w-1.5";
const indicatorClasses = "bg-primary select-none data-[orientation=horizontal]:h-full data-[orientation=vertical]:w-full";
function toArray(value) {
  if (value == null) return [];
  return Array.isArray(value) ? [...value] : [value];
}
function Slider({
  tooltipVisibility = "auto",
  thumbAriaLabel,
  className,
  value,
  defaultValue,
  orientation = "horizontal",
  onValueChange,
  ...rootProps
}) {
  const direction = useDirection();
  const dirSign = direction === "rtl" ? -1 : 1;
  const reduced = useReducedMotion();
  const seed = value ?? defaultValue;
  const thumbCount = Array.isArray(seed) ? seed.length : 1;
  const trackTooltips = tooltipVisibility !== "never";
  const prevValuesRef = React.useRef(toArray(seed));
  const [tilts, setTilts] = React.useState(
    () => Array.from({ length: thumbCount }, () => ({ direction: "up", version: 0 }))
  );
  const handleValueChange = (next, details) => {
    if (trackTooltips) {
      const nextArr = toArray(next);
      const prevArr = prevValuesRef.current;
      prevValuesRef.current = nextArr;
      setTilts((prev) => {
        let changed = false;
        const updated = nextArr.map((_, i) => {
          const existing = prev[i] ?? { direction: "up", version: 0 };
          const p = prevArr[i] ?? 0;
          const c = nextArr[i] ?? p;
          if (c === p) return existing;
          changed = true;
          return { direction: c > p ? "up" : "down", version: existing.version + 1 };
        });
        return changed || updated.length !== prev.length ? updated : prev;
      });
    }
    onValueChange?.(next, details);
  };
  const resolveAriaLabel = (index) => {
    if (typeof thumbAriaLabel === "function") return thumbAriaLabel(index);
    return thumbAriaLabel;
  };
  return /* @__PURE__ */ jsx(LazyMotion, { features: domAnimation, strict: true, children: /* @__PURE__ */ jsx(
    Slider$1.Root,
    {
      "data-slot": "slider",
      orientation,
      value,
      defaultValue,
      onValueChange: handleValueChange,
      className: cn(rootClasses, className),
      ...rootProps,
      children: /* @__PURE__ */ jsxs(Slider$1.Control, { "data-slot": "slider-control", className: controlClasses, children: [
        /* @__PURE__ */ jsx(Slider$1.Track, { "data-slot": "slider-track", className: trackClasses, children: /* @__PURE__ */ jsx(Slider$1.Indicator, { "data-slot": "slider-indicator", className: indicatorClasses }) }),
        Array.from({ length: thumbCount }).map((_, i) => /* @__PURE__ */ jsx(
          SliderThumb,
          {
            index: i,
            ariaLabel: resolveAriaLabel(i),
            tooltipVisibility,
            orientation,
            changeVersion: tilts[i]?.version ?? 0,
            direction: tilts[i]?.direction ?? "up",
            dirSign,
            reduced
          },
          i
        ))
      ] })
    }
  ) });
}

export { Slider };
//# sourceMappingURL=slider.js.map
