'use client';
import { jsx } from 'react/jsx-runtime';
import * as React from 'react';
import { OTPField as OTPField$1 } from '@base-ui/react/otp-field';
import { cn } from '../../internal/utils.js';
import { inputVariants } from '../input/input-variants.js';

const OTPFieldContext = React.createContext(null);
function useOTPFieldContext() {
  const ctx = React.useContext(OTPFieldContext);
  if (!ctx) {
    throw new Error("OTPField sub-components must be rendered inside <OTPField>");
  }
  return ctx;
}
const INPUT_SQUARE = {
  sm: "w-8",
  md: "w-10",
  lg: "w-12"
};
const SEPARATOR_SIZE = {
  sm: "w-3 [&_svg]:size-3",
  md: "w-4 [&_svg]:size-3.5",
  lg: "w-5 [&_svg]:size-4.5"
};
function OTPField({ variant = "outline", size = "md", className, children, ...props }) {
  const ctx = React.useMemo(() => ({ variant, size }), [variant, size]);
  return /* @__PURE__ */ jsx(OTPFieldContext.Provider, { value: ctx, children: /* @__PURE__ */ jsx(OTPField$1.Root, { "data-slot": "otp-field", className: cn("flex w-fit items-center gap-1", className), ...props, children }) });
}
function OTPFieldInput({ className, ...props }) {
  const { variant, size } = useOTPFieldContext();
  const ariaInvalid = props["aria-invalid"];
  const invalid = ariaInvalid === true || ariaInvalid === "true";
  return /* @__PURE__ */ jsx(
    OTPField$1.Input,
    {
      "data-slot": "otp-field-input",
      className: cn(
        inputVariants({ variant, size, state: "self" }),
        "px-0 text-center focus:placeholder:text-transparent",
        INPUT_SQUARE[size],
        className
      ),
      ...props,
      ...invalid ? { "data-invalid": "" } : {}
    }
  );
}
function OTPFieldSeparator({ className, children, ...props }) {
  const { size } = useOTPFieldContext();
  return /* @__PURE__ */ jsx(
    OTPField$1.Separator,
    {
      "data-slot": "otp-field-separator",
      className: cn("text-border-strong flex items-center justify-center", SEPARATOR_SIZE[size], className),
      ...props,
      children: children ?? /* @__PURE__ */ jsx(SeparatorMark, {})
    }
  );
}
function SeparatorMark() {
  return /* @__PURE__ */ jsx("svg", { "aria-hidden": "true", viewBox: "0 0 20 4", fill: "none", children: /* @__PURE__ */ jsx("path", { d: "M2 2H18", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round" }) });
}

export { OTPField, OTPFieldInput, OTPFieldSeparator };
//# sourceMappingURL=otp-field.js.map
