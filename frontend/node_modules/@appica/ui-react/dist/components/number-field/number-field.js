'use client';
import { jsxs, jsx } from 'react/jsx-runtime';
import * as React from 'react';
import { NumberField as NumberField$1 } from '@base-ui/react/number-field';
import { LazyMotion, domAnimation, AnimatePresence, m } from 'motion/react';
import { cn } from '../../internal/utils.js';
import { useReducedMotion } from '../../hooks/use-reduced-motion.js';
import { buttonVariants } from '../button/button-variants.js';
import { inputVariants } from '../input/input-variants.js';

const numberFieldWidth = {
  sm: "w-24",
  md: "w-30",
  lg: "w-35"
};
const stepperSize = {
  sm: { size: "icon-sm", override: "size-7 rounded-xs [&_svg]:size-3.5!" },
  md: { size: "icon-md", override: "size-9 rounded-sm [&_svg]:size-4!" },
  lg: { size: "icon-lg", override: "size-11 rounded-md [&_svg]:size-4.5!" }
};
const stepperButtonVariant = {
  outline: "soft",
  soft: "outline"
};
const inputSizeClasses = {
  sm: "text-xs pointer-coarse:text-base",
  md: "text-sm pointer-coarse:text-base",
  lg: "text-base"
};
const digitVariants = {
  enter: (dir) => ({
    y: dir === "up" ? "100%" : "-100%",
    opacity: 0
  }),
  center: { y: "0%", opacity: 1 },
  exit: (dir) => ({
    y: dir === "up" ? "-100%" : "100%",
    opacity: 0
  })
};
const DIGIT_TRANSITION = { duration: 0.22, ease: [0.4, 0, 0.2, 1] };
function AnimatedDigit({ char, direction }) {
  return /* @__PURE__ */ jsx("span", { className: "relative inline-block overflow-hidden", children: /* @__PURE__ */ jsx(AnimatePresence, { custom: direction, mode: "popLayout", initial: false, children: /* @__PURE__ */ jsx(
    m.span,
    {
      custom: direction,
      variants: digitVariants,
      initial: "enter",
      animate: "center",
      exit: "exit",
      transition: DIGIT_TRANSITION,
      className: "inline-block",
      children: char
    },
    char
  ) }) });
}
function AnimatedNumber({ value, direction }) {
  const chars = React.useMemo(() => Array.from(value), [value]);
  return /* @__PURE__ */ jsx("span", { className: "inline-flex leading-none", children: chars.map((ch, i) => {
    const fromRight = chars.length - 1 - i;
    return /* @__PURE__ */ jsx(AnimatedDigit, { char: ch, direction }, `r${fromRight}`);
  }) });
}
function NumberField({
  className,
  variant = "outline",
  size = "md",
  onValueChange,
  onValueCommitted,
  format,
  locale,
  value: controlledValue,
  defaultValue,
  min,
  placeholder = " ",
  inputProps,
  ...rootProps
}) {
  const resolvedVariant = variant;
  const resolvedSize = size;
  const stepperVariant = stepperButtonVariant[resolvedVariant];
  const { size: stepperBtnSize, override: stepperOverride } = stepperSize[resolvedSize];
  const stepperClass = cn(buttonVariants({ variant: stepperVariant, size: stepperBtnSize }), stepperOverride);
  const ariaInvalid = rootProps["aria-invalid"];
  const invalid = ariaInvalid === true || ariaInvalid === "true";
  const reduced = useReducedMotion();
  const formatter = React.useMemo(() => {
    try {
      return new Intl.NumberFormat(locale, format);
    } catch {
      return new Intl.NumberFormat();
    }
  }, [locale, format]);
  const formatValue = React.useCallback(
    (value) => value == null || Number.isNaN(value) ? "" : formatter.format(value),
    [formatter]
  );
  const isControlled = controlledValue !== void 0;
  const [internalValue, setInternalValue] = React.useState(defaultValue ?? null);
  const currentValue = isControlled ? controlledValue : internalValue;
  const [state, setState] = React.useState(() => ({
    value: formatValue(controlledValue ?? defaultValue ?? null),
    direction: "up"
  }));
  const [prevValue, setPrevValue] = React.useState(currentValue);
  if (prevValue !== currentValue) {
    setPrevValue(currentValue);
    const next = formatValue(currentValue);
    setState((prev) => prev.value === next ? prev : { ...prev, value: next });
  }
  const handleValueChange = (value, details) => {
    if (!isControlled) setInternalValue(value);
    setState((prev) => ({
      value: formatValue(value),
      direction: details.reason === "decrement-press" ? "down" : details.reason === "increment-press" ? "up" : prev.direction
    }));
    onValueChange?.(value, details);
  };
  const handleValueCommitted = (value, details) => {
    onValueCommitted?.(value, details);
    if (value != null) return;
    const fallback = min ?? 0;
    if (!isControlled) setInternalValue(fallback);
    setState((prev) => ({ ...prev, value: formatValue(fallback) }));
    const syntheticDetails = {
      reason: "none",
      event: details.event,
      cancel: () => {
      },
      allowPropagation: () => {
      },
      isCanceled: false,
      isPropagationAllowed: false,
      trigger: void 0
    };
    onValueChange?.(fallback, syntheticDetails);
  };
  const [isFocused, setIsFocused] = React.useState(false);
  const suppressFocusRef = React.useRef(false);
  const handleStepperPointerDown = () => {
    suppressFocusRef.current = true;
    setIsFocused(false);
    setTimeout(() => {
      suppressFocusRef.current = false;
    }, 0);
  };
  const handleInputPointerDown = () => setIsFocused(true);
  const handleInputFocus = () => {
    if (suppressFocusRef.current) return;
    setIsFocused(true);
  };
  const handleInputBlur = () => setIsFocused(false);
  return /* @__PURE__ */ jsxs(
    NumberField$1.Root,
    {
      "data-slot": "number-field",
      className: cn(
        inputVariants({ variant: resolvedVariant, size: resolvedSize, state: "within" }),
        "h-auto shrink-0 p-px",
        numberFieldWidth[resolvedSize],
        className
      ),
      onValueChange: handleValueChange,
      onValueCommitted: handleValueCommitted,
      format,
      locale,
      min,
      value: currentValue,
      ...rootProps,
      ...invalid ? { "data-invalid": "" } : {},
      children: [
        /* @__PURE__ */ jsx(
          NumberField$1.Decrement,
          {
            "data-slot": "number-field-decrement",
            "aria-label": "Decrease value",
            className: stepperClass,
            onPointerDown: handleStepperPointerDown,
            children: /* @__PURE__ */ jsx(MinusIcon, {})
          }
        ),
        /* @__PURE__ */ jsx(LazyMotion, { features: domAnimation, strict: true, children: /* @__PURE__ */ jsxs("div", { className: "relative flex-1 self-stretch overflow-hidden", children: [
          /* @__PURE__ */ jsx(
            NumberField$1.Input,
            {
              "data-slot": "number-field-input",
              placeholder,
              ...inputProps,
              onPointerDown: (e) => {
                inputProps?.onPointerDown?.(e);
                handleInputPointerDown();
              },
              onFocus: (e) => {
                inputProps?.onFocus?.(e);
                handleInputFocus();
              },
              onBlur: (e) => {
                inputProps?.onBlur?.(e);
                handleInputBlur();
              },
              className: cn(
                "peer text-foreground absolute inset-0 h-full w-full bg-transparent text-center outline-none",
                inputSizeClasses[resolvedSize],
                !reduced && !isFocused && "not-placeholder-shown:text-transparent not-placeholder-shown:caret-transparent",
                inputProps?.className
              )
            }
          ),
          !reduced && /* @__PURE__ */ jsx(
            "div",
            {
              "data-slot": "number-field-overlay",
              "aria-hidden": "true",
              className: cn(
                "text-foreground pointer-events-none absolute inset-0 z-10 flex items-center justify-center peer-placeholder-shown:invisible",
                isFocused && "invisible",
                inputSizeClasses[resolvedSize]
              ),
              children: /* @__PURE__ */ jsx(AnimatedNumber, { value: state.value, direction: state.direction })
            }
          )
        ] }) }),
        /* @__PURE__ */ jsx(
          NumberField$1.Increment,
          {
            "data-slot": "number-field-increment",
            "aria-label": "Increase value",
            className: stepperClass,
            onPointerDown: handleStepperPointerDown,
            children: /* @__PURE__ */ jsx(PlusIcon, {})
          }
        )
      ]
    }
  );
}
function MinusIcon() {
  return /* @__PURE__ */ jsx(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      "aria-hidden": "true",
      children: /* @__PURE__ */ jsx("path", { d: "M5 12h14" })
    }
  );
}
function PlusIcon() {
  return /* @__PURE__ */ jsx(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      "aria-hidden": "true",
      children: /* @__PURE__ */ jsx("path", { d: "M12 5v14M5 12h14" })
    }
  );
}

export { NumberField };
//# sourceMappingURL=number-field.js.map
