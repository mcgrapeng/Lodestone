'use client';
import { jsx, jsxs, Fragment } from 'react/jsx-runtime';
import * as React from 'react';
import { cn } from '../../internal/utils.js';
import { normalizeColor, describeColor } from '../../internal/color-control.js';
import { ColorPickerContext, useRequiredColorPickerContext } from '../../internal/color-picker-context.js';
import { ColorArea } from '../color-area/color-area.js';
import { ColorSlider } from '../color-slider/color-slider.js';
import { ColorSwatch } from '../color-swatch/color-swatch.js';
import { buttonVariants } from '../button/button-variants.js';
import { Popover, PopoverTrigger, PopoverContent } from '../popover/popover.js';
import { formatColor, safeParseColor, withChannelValue } from '../../lib/color.js';
import { Input } from '../input/input.js';

const DEFAULT_VALUE = "#ffffff";
const TRIGGER_SIZES = {
  // A swatch-only trigger is square, so its swatch takes the next size up.
  sm: { button: "sm", swatch: 20, iconSwatch: 24 },
  md: { button: "md", swatch: 24, iconSwatch: 28 },
  lg: { button: "lg", swatch: 28, iconSwatch: 32 }
};
const ICON_BUTTON_SIZES = {
  sm: "icon-sm",
  md: "icon-md",
  lg: "icon-lg"
};
const FLUSH_CLASSES = "size-auto rounded-none p-0 hover:before:bg-transparent data-popup-open:before:bg-transparent data-pressed:before:bg-transparent";
const SWATCH_OFFSETS = {
  sm: { start: "-ms-2.5", end: "-me-2.5" },
  md: { start: "-ms-3", end: "-me-3" },
  lg: { start: "-ms-3.5", end: "-me-3.5" }
};
const FORMAT_LABELS = {
  hex: "Hex",
  hexa: "Hex",
  rgb: "RGB",
  rgba: "RGB",
  hsl: "HSL",
  hsla: "HSL",
  hsb: "HSB",
  hsba: "HSB",
  oklch: "OKLCH",
  oklcha: "OKLCH"
};
const OPAQUE_FORMATS = {
  hexa: "hex",
  rgba: "rgb",
  hsla: "hsl",
  hsba: "hsb",
  oklcha: "oklch"
};
function displayFormat(format, color) {
  return color.alpha < 1 ? format : OPAQUE_FORMATS[format] ?? format;
}
const panelClasses = "flex w-fit min-w-50 flex-col gap-3";
function ColorPicker({
  value,
  defaultValue = DEFAULT_VALUE,
  onValueChange,
  onValueCommitted,
  children,
  format: formatProp,
  alpha = false,
  eyedropper = false,
  inline = false,
  disabled = false,
  size = "md",
  variant = "ghost",
  label,
  swatchShape = "rounded",
  swatchPosition = "start",
  trigger,
  open,
  defaultOpen = false,
  onOpenChange,
  side = "bottom",
  align = "start",
  sideOffset = 6,
  popoverProps,
  name,
  form,
  className,
  "aria-label": ariaLabel,
  ...props
}) {
  const [uncontrolled, setUncontrolled] = React.useState(() => normalizeColor(defaultValue));
  const [internalOpen, setInternalOpen] = React.useState(defaultOpen);
  const controlled = value !== void 0;
  const color = controlled ? normalizeColor(value) : uncontrolled;
  const format = formatProp ?? (alpha ? "hexa" : "hex");
  const latestRef = React.useRef({ controlled, onValueChange, onValueCommitted });
  latestRef.current = { controlled, onValueChange, onValueCommitted };
  const context = React.useMemo(
    () => ({
      value: color,
      format,
      disabled,
      setValue: (next) => {
        if (!latestRef.current.controlled) setUncontrolled(next);
        latestRef.current.onValueChange?.(next);
      },
      commitValue: (next) => latestRef.current.onValueCommitted?.(next)
    }),
    [color, format, disabled]
  );
  const panel = children ?? /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(ColorArea, { colorSpace: "hsb", xChannel: "saturation", yChannel: "brightness", "aria-label": "Saturation and brightness" }),
    /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3", children: [
      eyedropper && /* @__PURE__ */ jsx(ColorPickerEyeDropper, {}),
      /* @__PURE__ */ jsxs("div", { className: "flex min-w-0 flex-1 flex-col gap-2", children: [
        /* @__PURE__ */ jsx(ColorSlider, { channel: "hue" }),
        alpha && /* @__PURE__ */ jsx(ColorSlider, { channel: "alpha" })
      ] }),
      inline && /* @__PURE__ */ jsx(ColorSwatch, { size: "sm", shape: "circle", className: "shrink-0" })
    ] }),
    /* @__PURE__ */ jsx(ColorPickerInput, {})
  ] });
  const valueText = formatColor(color, displayFormat(format, color));
  const hiddenInput = name ? /* @__PURE__ */ jsx("input", { type: "hidden", name, form, value: valueText }) : null;
  if (inline) {
    return /* @__PURE__ */ jsx(ColorPickerContext.Provider, { value: context, children: /* @__PURE__ */ jsxs(
      "div",
      {
        role: "group",
        "aria-label": ariaLabel,
        ...props,
        "data-slot": "color-picker",
        ...disabled ? { "data-disabled": "" } : {},
        className: cn(panelClasses, className),
        children: [
          panel,
          hiddenInput
        ]
      }
    ) });
  }
  const description = describeColor(color);
  const iconOnly = label === null;
  const sizes = TRIGGER_SIZES[size];
  const swatch = /* @__PURE__ */ jsx(
    ColorSwatch,
    {
      "aria-hidden": "true",
      size: iconOnly ? sizes.iconSwatch : sizes.swatch,
      shape: swatchShape,
      className: iconOnly || variant === "flush" ? void 0 : SWATCH_OFFSETS[size][swatchPosition]
    }
  );
  const defaultTrigger = /* @__PURE__ */ jsxs("button", { type: "button", children: [
    swatchPosition === "start" && swatch,
    !iconOnly && /* @__PURE__ */ jsx("span", { children: label ?? valueText }),
    swatchPosition === "end" && swatch,
    !ariaLabel && /* @__PURE__ */ jsx("span", { className: "sr-only", children: description })
  ] });
  return /* @__PURE__ */ jsxs(ColorPickerContext.Provider, { value: context, children: [
    /* @__PURE__ */ jsxs(
      Popover,
      {
        open: open ?? internalOpen,
        onOpenChange: (next, details) => {
          onOpenChange?.(next, details);
          if (open === void 0 && !details.isCanceled) setInternalOpen(next);
        },
        children: [
          trigger !== null && /* @__PURE__ */ jsx(
            PopoverTrigger,
            {
              "aria-label": ariaLabel && `${ariaLabel}, ${description}`,
              ...props,
              "data-slot": "color-picker-trigger",
              disabled,
              className: cn(
                !trigger && [
                  buttonVariants({
                    variant: variant === "flush" ? "ghost" : variant,
                    size: iconOnly ? ICON_BUTTON_SIZES[size] : sizes.button
                  }),
                  variant === "flush" && FLUSH_CLASSES
                ],
                className
              ),
              render: trigger ?? defaultTrigger
            }
          ),
          /* @__PURE__ */ jsx(
            PopoverContent,
            {
              side,
              align,
              sideOffset,
              arrow: false,
              "aria-label": ariaLabel ?? "Color picker",
              ...popoverProps,
              "data-slot": "color-picker-panel",
              className: cn(panelClasses, "max-w-none p-3", popoverProps?.className),
              children: panel
            }
          )
        ]
      }
    ),
    hiddenInput
  ] });
}
function parseInputColor(text) {
  const trimmed = text.trim();
  return safeParseColor(/^[0-9a-f]{3,8}$/i.test(trimmed) ? `#${trimmed}` : trimmed);
}
function ColorPickerInput({ format: formatProp, className, ...props }) {
  const picker = useRequiredColorPickerContext("ColorPickerInput");
  const format = formatProp ?? picker.format;
  const [draft, setDraft] = React.useState(null);
  const commit = () => {
    const parsed = draft === null ? void 0 : parseInputColor(draft);
    if (parsed) picker.commitValue(parsed);
    setDraft(null);
  };
  return /* @__PURE__ */ jsx(
    Input,
    {
      inputSize: "sm",
      spellCheck: false,
      autoComplete: "off",
      autoCorrect: "off",
      autoCapitalize: "off",
      "aria-label": FORMAT_LABELS[format],
      ...props,
      "data-slot": "color-picker-input",
      inputProps: { size: 1, ...props.inputProps },
      disabled: props.disabled ?? picker.disabled,
      value: draft ?? formatColor(picker.value, displayFormat(format, picker.value)),
      className: cn("font-mono", className),
      onChange: (event) => {
        const next = event.target.value;
        setDraft(next);
        const parsed = parseInputColor(next);
        if (parsed) picker.setValue(parsed);
      },
      onBlur: (event) => {
        props.onBlur?.(event);
        commit();
      },
      onKeyDown: (event) => {
        props.onKeyDown?.(event);
        if (event.defaultPrevented) return;
        if (event.key === "Enter") {
          event.preventDefault();
          commit();
        } else if (event.key === "Escape") {
          setDraft(null);
        }
      }
    }
  );
}
function ColorPickerEyeDropper({
  variant = "ghost",
  size = "icon-sm",
  className,
  disabled,
  children = /* @__PURE__ */ jsx(PipetteIcon, {}),
  "aria-label": ariaLabel = "Pick a color from the screen",
  ...props
}) {
  const picker = useRequiredColorPickerContext("ColorPickerEyeDropper");
  const [supported, setSupported] = React.useState(false);
  React.useEffect(() => setSupported("EyeDropper" in window), []);
  if (!supported) return null;
  const off = disabled ?? picker.disabled;
  const sample = async () => {
    const EyeDropper = window.EyeDropper;
    let result;
    try {
      result = await new EyeDropper().open();
    } catch {
      return;
    }
    const sampled = safeParseColor(result.sRGBHex);
    if (!sampled) return;
    const next = withChannelValue(sampled, "alpha", picker.value.alpha);
    picker.setValue(next);
    picker.commitValue(next);
  };
  return /* @__PURE__ */ jsx(
    "button",
    {
      type: "button",
      "aria-label": ariaLabel,
      ...props,
      "data-slot": "color-picker-eyedropper",
      disabled: off,
      ...off ? { "data-disabled": "" } : {},
      className: cn(
        buttonVariants({ variant, size }),
        "text-foreground-muted hover:text-foreground shrink-0",
        className
      ),
      onClick: (event) => {
        props.onClick?.(event);
        if (!event.defaultPrevented) void sample();
      },
      children
    }
  );
}
function PipetteIcon() {
  return /* @__PURE__ */ jsxs(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      "aria-hidden": "true",
      children: [
        /* @__PURE__ */ jsx("path", { d: "m2 22 1-1h3l9-9" }),
        /* @__PURE__ */ jsx("path", { d: "M3 21v-3l9-9" }),
        /* @__PURE__ */ jsx("path", { d: "m15 6 3.4-3.4a2.1 2.1 0 1 1 3 3L18 9l.4.4a1 1 0 1 1-3 3l-3.8-3.8a1 1 0 1 1 3-3l.4.4Z" })
      ]
    }
  );
}

export { ColorPicker, ColorPickerEyeDropper, ColorPickerInput };
//# sourceMappingURL=color-picker.js.map
