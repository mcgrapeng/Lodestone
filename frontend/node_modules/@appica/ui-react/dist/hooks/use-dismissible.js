'use client';
import { useCallback, useMemo } from 'react';
import { useStorage } from './use-local-storage.js';

function useDismissible(key, { storage = "local" } = {}) {
  const [flag, setFlag, removeFlag] = useStorage(storage, key, "", {
    serializer: (v) => v,
    deserializer: (v) => v
  });
  const open = flag !== "1";
  const dismiss = useCallback(() => setFlag("1"), [setFlag]);
  const reset = useCallback(() => removeFlag(), [removeFlag]);
  return useMemo(() => ({ open, dismiss, reset }), [open, dismiss, reset]);
}

export { useDismissible };
//# sourceMappingURL=use-dismissible.js.map
