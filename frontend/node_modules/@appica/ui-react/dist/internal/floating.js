const FLOATING_POSITIONER_KEYS = [
  "side",
  "sideOffset",
  "align",
  "alignOffset",
  "anchor",
  "positionMethod",
  "collisionBoundary",
  "collisionPadding",
  "collisionAvoidance",
  "sticky",
  "arrowPadding",
  "disableAnchorTracking"
];
const FLOATING_PORTAL_KEYS = ["container", "keepMounted"];
function splitFloatingProps(props) {
  const { positionerProps, portalProps, ...rest } = props;
  const positioner = { ...positionerProps };
  const portal = { ...portalProps };
  const popup = {};
  for (const key of Object.keys(rest)) {
    const value = rest[key];
    if (FLOATING_POSITIONER_KEYS.includes(key)) {
      if (value !== void 0) positioner[key] = value;
    } else if (FLOATING_PORTAL_KEYS.includes(key)) {
      if (value !== void 0) portal[key] = value;
    } else {
      popup[key] = value;
    }
  }
  return { positioner, portal, popup };
}

export { FLOATING_PORTAL_KEYS, FLOATING_POSITIONER_KEYS, splitFloatingProps };
//# sourceMappingURL=floating.js.map
