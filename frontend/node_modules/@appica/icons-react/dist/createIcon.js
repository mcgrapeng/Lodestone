import { jsx } from "react/jsx-runtime";
const BASE_CLASS = "appica-icon";
function createIcon(name, svgContent, options = {}) {
  const {
    viewBox = "0 0 24 24",
    fill = "currentColor",
    stroke,
    strokeWidth,
    strokeLinecap,
    strokeLinejoin,
    strokeMiterlimit
  } = options;
  function Component({
    size = 24,
    className,
    "aria-hidden": ariaHidden = "true",
    ref,
    ...props
  }) {
    return /* @__PURE__ */ jsx(
      "svg",
      {
        ref,
        xmlns: "http://www.w3.org/2000/svg",
        viewBox,
        fill,
        stroke,
        strokeWidth,
        strokeLinecap,
        strokeLinejoin,
        strokeMiterlimit,
        width: size,
        height: size,
        "aria-hidden": ariaHidden,
        className: className ? `${BASE_CLASS} ${className}` : BASE_CLASS,
        ...props,
        dangerouslySetInnerHTML: { __html: svgContent }
      }
    );
  }
  Component.displayName = name;
  return Component;
}
export {
  createIcon
};
