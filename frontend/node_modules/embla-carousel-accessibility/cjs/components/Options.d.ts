import { CreateOptionsType } from 'embla-carousel';
export type RootNodeType = null | ((emblaRoot: HTMLElement) => HTMLElement | null);
export type AriaTextCallbackType = (hasAnyGroupedSlides: boolean, firstSlideIndex: number, lastSlideIndex: number, totalSlides: number, selectedSnapIndex: number, totalSnaps: number) => string;
export type OptionsType = CreateOptionsType<{
    announceChanges: boolean;
    carouselAriaLabel: string;
    carouselAriaRoleDescription: string;
    carouselRole: string;
    previousButtonAriaLabel: string;
    nextButtonAriaLabel: string;
    slideAriaRoleDescription: string;
    slideRole: string;
    rootNode: RootNodeType;
    slideAriaLabel: AriaTextCallbackType;
    dotButtonAriaLabel: AriaTextCallbackType;
    liveRegionContent: AriaTextCallbackType;
}>;
export declare const defaultOptions: OptionsType;
